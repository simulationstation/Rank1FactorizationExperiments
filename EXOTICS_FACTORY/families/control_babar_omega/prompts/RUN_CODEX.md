# Codex prompt: BaBar control omega(1420)/omega(1650)

Build only the BaBar control omega(1420)/omega(1650) pipeline using the existing spec.yaml, without running any downloads, extraction, or tests. Ensure DRY_RUN is the default.

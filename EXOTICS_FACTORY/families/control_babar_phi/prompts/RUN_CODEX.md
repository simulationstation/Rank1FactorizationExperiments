# Codex prompt: BaBar control phi(1680)/phi(2170)

Build only the BaBar control phi(1680)/phi(2170) pipeline using the existing spec.yaml, without running any downloads, extraction, or tests. Ensure DRY_RUN is the default.

# Codex prompt: X(3872)-like channel sharing

Build only the X(3872)-like channel sharing pipeline using the existing spec.yaml, without running any downloads, extraction, or tests. Ensure DRY_RUN is the default.

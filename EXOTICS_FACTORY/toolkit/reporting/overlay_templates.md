# Overlay Templates

Use these placeholders for overlay proof images:

- `overlay_vector.png`: vector extraction overlay on PDF
- `overlay_raster.png`: raster segmentation overlay
- `overlay_combined.png`: combined overlay for QA
